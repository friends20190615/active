<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.active.front.dao.DriveDao">    <select id="getInfo" resultType="DriveInfo" parameterType="DriveInfo">        select id,name,mobile,id_number as idNumber,appointment,city_name as cityName,        car_modal as carModal,dealer,create_time as createTime,update_time as updateTime,state        from t_drive where mobile = #{mobile}    </select>    <insert id="insertInfo" parameterType="DriveInfo">        insert into t_drive(name,mobile,id_number,appointment,city_name,car_modal,dealer,create_time,state)values(#{name},#{mobile},#{idNumber},#{appointment},#{cityName},#{carModal},#{dealer},now(),1)    </insert>    <update id="updateInfo" parameterType="DriveInfo">        <![CDATA[			UPDATE t_drive SET				name = #{name}				,mobile = #{mobile}				,id_number = #{idNumber}				,appointment=#{appointment}				,city_name = #{cityName}				,car_modal=#{carModal}				,dealer=#{dealer}				,modify_time = now()				,state=#{state}			WHERE id = #{id}			]]>    </update>    <delete id="deleteVisitInfo" parameterType="DriveInfo">        DELETE FROM t_drive WHERE id = #{id}    </delete></mapper>