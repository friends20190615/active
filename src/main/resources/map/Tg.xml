<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.active.front.dao.TgDao">    <select id="getInfos" resultType="TgInfo" parameterType="TgInfo">        select id,name,mobile,status,        create_time as createTime,        update_time as updateTime from t_tiangong_active    </select>    <select id="getInfo" resultType="TgInfo" parameterType="string">        select id,name,mobile,status,        create_time as createTime,        update_time as updateTime from t_tiangong_active where mobile=#{mobile}    </select>    <select id="getCount" resultType="int" parameterType="int">        select count(id) as count from t_tiangong_active where status=#{status}    </select>    <insert id="addInfo"  parameterType="TgInfo">        insert into t_tiangong_active (name,mobile,status,create_time,update_time)        values (#{name},#{mobile},#{status},now(),now())    </insert></mapper>